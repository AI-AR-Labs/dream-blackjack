{
  "name": "dream-skeleton",
  "version": "1.0.0",
  "description": "AI-driven development workflow skeleton",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "dev": "node src/index.js",
    "build": "echo 'No build needed for serverless'",
    "start": "node src/index.js",
    "lint": "eslint src tests",
    "format": "prettier --write \"**/*.{js,json,md}\""
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "eslint": "^8.57.0",
    "jest": "^29.5.0",
    "prettier": "^3.0.0",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "express": "^4.18.2",
    "serverless-http": "^3.2.0"
  }
}